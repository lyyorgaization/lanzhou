<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lucien.dap.data.server.mapper.extend.DataEntityExtendMapper">
    <resultMap id="StatisticMap" type="com.lucien.dap.data.server.entity.extend.DataStatisticEntity">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="max" jdbcType="FLOAT" property="max"/>
        <result column="min" jdbcType="FLOAT" property="min"/>
        <result column="avg" jdbcType="FLOAT" property="avg"/>
    </resultMap>

    <select id="getStatistic" resultMap="StatisticMap">
        select max(${column}) as max,min(${column}) as min,avg(${column}) as avg from datas
        order by time desc
        limit #{limit}
    </select>

    <select id="getLast" resultType="float">
        select ${column} as avg from datas
        order by time desc
        limit 1
    </select>
</mapper>