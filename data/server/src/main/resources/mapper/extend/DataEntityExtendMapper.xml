<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lucien.dap.data.server.mapper.extend.DataEntityExtendMapper">
    <resultMap id="StatisticMap" type="com.lucien.dap.data.server.entity.extend.DataStatisticEntity">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="max" jdbcType="VARCHAR" property="max"/>
        <result column="min" jdbcType="VARCHAR" property="min"/>
        <result column="avg" jdbcType="VARCHAR" property="avg"/>
    </resultMap>

    <select id="getStatistic" resultMap="StatisticMap">
        select round(max(${column}),2) as max,round(min(${column}),2) as min,round(avg(${column}),2) as avg from
(select * from
datas
        order by time desc
        limit ${limit}
				) t
    </select>

    <select id="getLast" resultType="String">
        select round(${column},2) as avg from datas
        order by time desc
        limit 1
    </select>
</mapper>